cluster.name: "elkg-cluster"
network.host: 0.0.0.0

# Single-node lab setup — not for production
discovery.type: single-node

# Security is disabled here purely to keep this educational stack simple.
# Re-enable xpack.security.enabled if you need it
xpack.security.enabled: false
